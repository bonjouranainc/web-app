import React from 'react';

const Header = ({ title }) => (
  <header>
    <h1>{title}</h1>
    <h3>Compra/Subscripcion de servicio de limpieza y servicios adicionales</h3>
  </header>
);

export default Header;
